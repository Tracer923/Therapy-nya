<%= form_with(url: post_comments_path(post), local: true) do |f| %>
  <%= f.text_area :content, rows:'5',placeholder: "コメントをここに", class: "w-100" %>
  <%= f.submit "コメント！！", class: "btn btn-lg btn-base-1 mt-20 pull-right" %>
<% end %>